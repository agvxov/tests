#include <iostream>
#include <stdlib.h>
#include <string.h>
#include <limits.h>

using namespace std;

signed main(int argc, char* argv[]){

	//char c[128];
	//char* e = c;

	//memcpy(c, "asd", INT_MAX);
	////e += 3;

	//printf("%s", c);

	char dest[60];
	memcpy(dest, "why?", INT_MAX);

	return 0;
}

