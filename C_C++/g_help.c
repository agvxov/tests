#include<stdio.h>

signed main(){
	int exit = 0;
	int sel = 0;
	int input_decimal = 0;

	while(exit == 0)
	{
		scanf("%d", &sel);

		if(sel == 1)
		{
			printf("----Decimal to Binary----\n");
			printf("Input a decimal number up to 4,294,967,295: \n");
			scanf("%d", &input_decimal);
			printf("Sneed");
		}
	}
}
